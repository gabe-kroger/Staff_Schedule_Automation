1.  cd into the root directory and use npm run server to run the server
2.  cd into the client directory and use npm start to run the frontend
3.  in the root directory, use npm run dev to start frontend and backend simultaneously


